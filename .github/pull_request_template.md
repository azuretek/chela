<!-- One line per thing that matters. Delete a section only when it genuinely does not apply. -->

## What Problem This Solves

<!-- The behaviour that was wrong, who it hurt, and how it was observed. A measurement beats a description: a log line, a failing test, a number. -->

## Change

<!-- What the patch does, in mechanism order, naming the file or function each part lives in. Say which alternative was rejected and why, when one was. -->

## Test

<!-- The command, then its output. Name the case that fails without the patch, so the test is shown to bite. Say what was NOT run. -->
