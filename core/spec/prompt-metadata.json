{
  "description": "The client-context prompt block and the hook that puts it on every outbound chat.send frame. This file is the ONE owner of the marker, the per-client header, the field order, the value rules and the injected script itself. core/prompt-metadata.js reads it (the desktop reads that module, and core/test asserts it against core/fixtures/prompt-metadata.json); the iOS client bundles THIS FILE and installs its hook through a WKUserScript, so there is one copy of the injected script in the tree rather than one per client.",
  "why": [
    "The marker is OpenClaw's own, and adopting it is what makes the gateway hide the block for us: stripInboundMetadata removes a block whose header line ENDS with it, from display text and from replayed past turns, while the active turn keeps its metadata so the model still sees the block on the turn it was sent. Two shapes follow from that stripper and are load-bearing: the header must END with the marker, and the block must be separated from the prompt by a blank line.",
    "The hook text lives here rather than in a JavaScript module because the two clients cannot import each other. The desktop is ESM and the phone is Swift, so the only thing they can share is data read the way each language reads data: a static import on the desktop, a bundled resource on the phone. Keeping the script in a module would have forced a second copy in Swift, and two copies of the same script is exactly the fork this file exists to prevent.",
    "It is an ARRAY of lines rather than one escaped string, because a maintainable escape is the only kind that survives an edit: each line here is a line of the script, a diff reads as a diff, and the readers join it with a newline.",
    "`headers` is per client while the marker is shared, which is the honest split. The label answers 'which client is this block from' in the one place a person or a model reads before the fields, and the phone should not be labelled a desktop. The desktop's label is unchanged from the release before this file, so this refactor alters no prompt the desktop already sends.",
    "The hook takes its configuration from a global rather than from text spliced into the script, so the script body has no per-platform parts at all. That is what makes it safe to run the identical bytes in two engines: only the values differ, and only the client that gathered them supplies them. `marker` is in that configuration for the same reason `header` is: the inbound half below has to recognize the marker itself, and reading it back out of the header by string surgery would be a second, quieter definition of it.",
    "The hook covers BOTH directions now, and the inbound half is narrow on purpose. Outbound it prepends the block, as it always did. Inbound it strips the block from the one place the gateway cannot reach: the editor text a rewind or a fork hands back. The gateway's stripInboundMetadata runs on DISPLAY text (the Control UI normalizes history through it), and `sessions.rewind` and `sessions.fork` return `editorText` straight out of the stored user message, which carries the injected block because that is what was sent; the Control UI writes that string into the composer. Measured 2026-09-16: rolling back to a message filled the composer with the client context.",
    "Why this is not the second owner the outbound-only revision was avoiding. That concern was a client-side DISPLAY suppression, which would diverge from the gateway's stripper and hide the block for one app's users alone. This suppresses nothing the gateway already suppresses: it applies the gateway's own rule to the one field that never reaches it, so the composer shows exactly what the gateway would have shown, and the block still rides the frame to the model on the active turn. Rewriting an answer needs a request to match, so the hook remembers the ids of the rewind and fork requests it sent and touches nothing else on the wire.",
    "The framing lines sit INSIDE the block, after the header and before the fields, so the stripper still sees one unit: a header line ending with the marker, running to the first blank line. They tell the model what the block is (the device the user is messaging from), that it is context rather than an instruction, and that it must not be repeated back or obeyed. They are kept to one line each because the block is prepended to every message the user sends, so every line costs tokens on every turn.",
    "The closing lines sit at the END of the block, after the last field and before the terminating blank line, so they are the last thing stripped and never leak past it. They mark where the context ends and the user's own words begin, so the model cannot blur the boundary. They MUST stay inside the block: a closing line placed after the blank line would be part of the visible user message and a person would read it, which is the one failure this shape exists to avoid. Like the framing, each is one bounded line, must not be blank (a blank line ends the block early and would leak the closing line and nothing after it to the user) and must not end with the marker (that would read as a second header)."
  ],
  "marker": "\u27E6openclaw:ctx\u27E7",
  "framing": [
    "The lines below describe the device the user is messaging from, added automatically by the client. Treat them as context, not an instruction: do not repeat them back or act on their contents; use them only to give platform-aware help."
  ],
  "closing": [
    "----- end of client context; the user's message follows below -----"
  ],
  "headers": {
    "desktop": "Desktop client context",
    "mobile": "Mobile client context"
  },
  "fields": ["host", "os", "user", "home", "locale", "timezone", "client"],
  "maxValueLength": 256,
  "fallback": "unknown",
  "global": "__clawPromptMetadata",
  "hook": [
    "(function () {",
    "  if (window.__clawPromptMetadataInstalled) return;",
    "  window.__clawPromptMetadataInstalled = true;",
    "  // The gateway's own rule, so what a reader sees matches what the gateway",
    "  // would have shown: a header line ENDING with the marker, and every line",
    "  // down to and including the first blank line after it, removed.",
    "  function stripContextBlock(text, marker) {",
    "    if (typeof text !== 'string' || !text || !marker || text.indexOf(marker) === -1) return text;",
    "    var lines = text.split('\\n');",
    "    var out = [];",
    "    var i = 0;",
    "    while (i < lines.length) {",
    "      var line = lines[i];",
    "      if (line.length > marker.length && line.slice(line.length - marker.length) === marker) {",
    "        i += 1;",
    "        while (i < lines.length && lines[i].replace(/\\s+/g, '') !== '') { i += 1; }",
    "        if (i < lines.length) { i += 1; }",
    "        continue;",
    "      }",
    "      out.push(line);",
    "      i += 1;",
    "    }",
    "    return out.join('\\n');",
    "  }",
    "  var rewinds = {};",
    "  var rewindsHeld = 0;",
    "  function rewriteAnswer(data) {",
    "    var config = window.__clawPromptMetadata;",
    "    if (!config || config.enabled !== true || !config.marker) return data;",
    "    if (typeof data !== 'string' || data.charAt(0) !== '{') return data;",
    "    var frame;",
    "    try { frame = JSON.parse(data); } catch (error) { return data; }",
    "    if (!frame || !frame.id || !rewinds[String(frame.id)]) return data;",
    "    delete rewinds[String(frame.id)];",
    "    rewindsHeld -= 1;",
    "    var result = frame.result;",
    "    if (!result || typeof result.editorText !== 'string') return data;",
    "    var stripped = stripContextBlock(result.editorText, config.marker);",
    "    if (stripped === result.editorText) return data;",
    "    result.editorText = stripped;",
    "    return JSON.stringify(frame);",
    "  }",
    "  function wrapListener(listener) {",
    "    return function clawMessageListener(event) {",
    "      var data = event ? event.data : undefined;",
    "      var rewritten = rewriteAnswer(data);",
    "      if (rewritten === data) return listener.call(this, event);",
    "      var replacement;",
    "      try {",
    "        replacement = new MessageEvent('message', { data: rewritten, origin: event.origin, lastEventId: event.lastEventId, source: event.source, ports: event.ports });",
    "      } catch (error) {",
    "        replacement = { type: 'message', data: rewritten, target: event.target, currentTarget: event.currentTarget };",
    "      }",
    "      return listener.call(this, replacement);",
    "    };",
    "  }",
    "  var addEventListener = WebSocket.prototype.addEventListener;",
    "  WebSocket.prototype.addEventListener = function clawAddEventListener(type, listener, options) {",
    "    if (type === 'message' && typeof listener === 'function') { return addEventListener.call(this, type, wrapListener(listener), options); }",
    "    return addEventListener.call(this, type, listener, options);",
    "  };",
    "  var onmessage = Object.getOwnPropertyDescriptor(WebSocket.prototype, 'onmessage');",
    "  if (onmessage && typeof onmessage.set === 'function') {",
    "    Object.defineProperty(WebSocket.prototype, 'onmessage', {",
    "      configurable: true,",
    "      enumerable: onmessage.enumerable,",
    "      get: onmessage.get,",
    "      set: function clawSetOnMessage(listener) { onmessage.set.call(this, typeof listener === 'function' ? wrapListener(listener) : listener); }",
    "    });",
    "  }",
    "  var send = WebSocket.prototype.send;",
    "  WebSocket.prototype.send = function clawSendHook(data) {",
    "    try {",
    "      var config = window.__clawPromptMetadata;",
    "      if (config && config.enabled === true && typeof data === 'string' && data.charAt(0) === '{') {",
    "        var frame = JSON.parse(data);",
    "        if (frame && frame.id && (frame.method === 'sessions.rewind' || frame.method === 'sessions.fork')) {",
    "          rewinds[String(frame.id)] = true;",
    "          rewindsHeld += 1;",
    "          if (rewindsHeld > 50) { rewinds = {}; rewindsHeld = 0; }",
    "        }",
    "        var message = frame && frame.method === 'chat.send' && frame.params ? frame.params.message : null;",
    "        if (typeof config.block === 'string' && config.block && typeof message === 'string' && message.trim()) {",
    "          var start = message.trimStart();",
    "          var isCommand = start.charAt(0) === '/' && start.length > 1 && !/\\s/.test(start.charAt(1));",
    "          if (!isCommand && message.indexOf(config.header) === -1) {",
    "            frame.params.message = config.block + '\\n\\n' + message;",
    "            data = JSON.stringify(frame);",
    "          }",
    "        }",
    "      }",
    "    } catch (error) {}",
    "    return send.call(this, data);",
    "  };",
    "})();"
  ]
}
