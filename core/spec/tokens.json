{
  "description": "The design tokens Chela's own chrome draws with, so the notice banner on every client is the Control UI's card rather than a lookalike. This file is the ONE owner: desktop reads it as data (core/tokens.js) and iOS mirrors it as constants (mobile/Chela/NoticeTokens.swift), with a parity test asserting the mirror still equals this file.",
  "why": [
    "The gateway serves the Control UI's stylesheet as a content-hashed asset under an immutable cache header, for example /assets/index-Cuu-imEh.js, and our chrome is a separate document, so that URL cannot be imported and must not be hardcoded.",
    "What is genuinely shareable is the token layer. These are the values the Control UI resolves those tokens to, copied here with their source, so a drift is findable rather than invisible.",
    "Nothing reads the gateway at runtime for this. A value here is a fallback that a live Control UI theme overrides where the names overlap (see desktop/src/chrome.js THEME_TOKENS), which is what keeps our chrome matching whichever palette the UI is actually running."
  ],
  "source": {
    "repo": "openclaw",
    "checkout": "the OpenClaw checkout, not this repo",
    "files": [
      "ui/src/styles/base.css (the :root block is the dark palette, :root:where([data-theme-mode=\"light\"]) is the light one)",
      "ui/src/styles/sidebar-issues.css (the attention card's shape, type and controls)",
      "ui/src/styles/sidebar-attention-floating.css (the floating card's blur and hairline edge)"
    ]
  },
  "live": {
    "description": "The tokens a client may take from a RUNNING Control UI, by name and kind, so both clients follow whichever palette the UI is actually wearing rather than only the default one. The desktop reads these from the gateway page and inserts them into our own pages (desktop/src/chrome.js THEME_TOKENS, injected by main.js applyThemeCss); the phone reads the same names from the same page and applies them to the settings and About surfaces. The names the UI does not publish (--danger, the card geometry) have no live value to take, so for those ui.css's own declarations stand.",
    "whyItIsAnAllowlist": [
      "This list is not documentation: it is the filter. A gateway page reports whatever its theme has, and desktop/src/chrome.js sanitizeTokens keeps only the names here, so a palette token missing from this list cannot reach any of our pages however the Control UI defines it. The surface then keeps ui.css's copy of the Control UI's DEFAULT palette, and the page wears two palettes at once.",
      "That is why --card and --bg-elevated are named here (2026-09-16) and why dropping one back out is a regression rather than a tidy-up. They were missing while ui.css's own comment already claimed a running Control UI overrides them, so on any palette that is not the default the settings GROUPS were painted from the default one: measured, the group read rgb(22, 25, 32) while the purple palette in force published rgb(31, 29, 46) for --card, inside a window that was itself the purple. The borrowed components are what made it visible: their surface IS this token, where ours was --panel, which the live theme can reach.",
      "The guard is core/test/live-tokens.test.js: every colour ui.css declares as a palette fallback must be a name here or a stated exception, and the measured half is desktop/scripts/test-settings-theme.js, which opens our settings surface in the real app against a published palette and requires every declared colour to resolve to it. That harness is what fails when a name is dropped from this list: the page keeps ui.css's fallback for it, which is a two-palette page nobody can see in a diff."
    ],
    "ours": [
      { "name": "--focus-ring", "reason": "ours: upstream publishes one, but this is the ring our own controls draw with" },
      { "name": "--ok", "reason": "ours: the Control UI has no --ok to take (see the tone note in the token spec)" },
      { "name": "--warn", "reason": "ours: the Control UI has no --warn to take (see the tone note in the token spec)" },
      { "name": "--scrim", "reason": "ours: the Control UI has no scrim to take, and this page writes its own" }
    ],
    "tokens": [
      ["--bg", "color"], ["--bg-accent", "color"], ["--bg-hover", "color"],
      ["--bg-muted", "color"], ["--bg-content", "color"], ["--bg-elevated", "color"], ["--popover", "color"], ["--popover-foreground", "color"],
      ["--panel", "color"], ["--panel-hover", "color"], ["--panel-strong", "color"],
      ["--card", "color"],
      ["--input", "color"], ["--chrome", "color"],
      ["--text", "color"], ["--text-strong", "color"],
      ["--muted", "color"], ["--muted-strong", "color"],
      ["--border", "color"], ["--border-strong", "color"], ["--border-hover", "color"],
      ["--accent", "color"], ["--accent-hover", "color"], ["--accent-subtle", "color"],
      ["--primary", "color"], ["--primary-hover", "color"], ["--primary-foreground", "color"],
      ["--destructive", "color"], ["--ring", "color"],
      ["--radius", "length"], ["--radius-sm", "length"], ["--radius-md", "length"],
      ["--radius-lg", "length"], ["--radius-full", "length"],
      ["--scrollbar-size", "length"], ["--scrollbar-thumb-inset", "length"],
      ["--scrollbar-thumb", "color"], ["--scrollbar-thumb-hover", "color"],
      ["--font-body", "font"], ["--shadow-lg", "shadow"],
      ["--control-ui-text-scale", "scale"]
    ]
  },
  "css": {
    "dark": {
      "--bg": "#0e1015",
      "--bg-elevated": "#191c24",
      "--popover": "#191c24",
      "--popover-foreground": "#f0f0f2",
      "--bg-hover": "#1f2330",
      "--bg-muted": "#1f2330",
      "--panel": "#0e1015",
      "--panel-strong": "#191c24",
      "--card": "#161920",
      "--text": "#bcbcc0",
      "--text-strong": "#f4f4f5",
      "--muted": "#8b8b94",
      "--border": "#1e2028",
      "--border-strong": "#2e3040",
      "--accent": "#ff5c5c",
      "--accent-hover": "#ff7070",
      "--accent-subtle": "rgba(255, 92, 92, 0.1)",
      "--accent-foreground": "#fafafa",
      "--ring": "#ff5c5c",
      "--ok": "#22c55e",
      "--ok-subtle": "rgba(34, 197, 94, 0.08)",
      "--warn": "#f59e0b",
      "--warn-subtle": "rgba(245, 158, 11, 0.08)",
      "--danger": "#f87171",
      "--danger-subtle": "rgba(248, 113, 113, 0.08)",
      "--info": "#60a5fa",
      "--info-subtle": "rgba(96, 165, 250, 0.08)"
    },
    "light": {
      "--bg": "#faf9f7",
      "--bg-elevated": "#ffffff",
      "--popover": "#ffffff",
      "--popover-foreground": "#211e1a",
      "--bg-hover": "#efebe4",
      "--bg-muted": "#efebe4",
      "--panel": "#faf9f7",
      "--panel-strong": "#f4f1ec",
      "--card": "#ffffff",
      "--text": "#403c35",
      "--text-strong": "#211e1a",
      "--muted": "#6e6960",
      "--border": "#e8e4dc",
      "--border-strong": "#d6d0c5",
      "--accent": "#bd4531",
      "--accent-hover": "#a83c29",
      "--accent-subtle": "rgba(189, 69, 49, 0.08)",
      "--accent-foreground": "#ffffff",
      "--ring": "#bd4531",
      "--ok": "#166534",
      "--ok-subtle": "rgba(22, 101, 52, 0.08)",
      "--warn": "#92400e",
      "--warn-subtle": "rgba(146, 64, 14, 0.08)",
      "--danger": "#b91c1c",
      "--danger-subtle": "rgba(185, 28, 28, 0.08)",
      "--info": "#1d4ed8",
      "--info-subtle": "rgba(29, 78, 216, 0.08)"
    }
  },
  "provenance": {
    "--bg": "base.css :root --bg; light from :root:where([data-theme-mode=\"light\"]) --bg",
    "--bg-elevated": "base.css :root --bg-elevated, the surface the attention card paints",
    "--popover": "base.css :root --popover, the surface the Control UI toast paints",
    "--popover-foreground": "base.css :root --popover-foreground, the words on the toast",
    "--bg-hover": "base.css :root --bg-hover, the card control's hover fill",
    "--bg-muted": "base.css :root --bg-muted, the toast X's hover fill",
    "--panel": "base.css :root --panel",
    "--panel-strong": "base.css :root --panel-strong",
    "--card": "base.css :root --card",
    "--text": "base.css :root --text",
    "--text-strong": "base.css :root --text-strong, the headline colour",
    "--muted": "base.css :root --muted, the subject line and the dismiss glyph",
    "--border": "base.css :root --border, the card's hairline",
    "--border-strong": "base.css :root --border-strong",
    "--accent": "base.css :root --accent",
    "--accent-hover": "base.css :root --accent-hover",
    "--accent-subtle": "base.css :root --accent-subtle, reused for the info tone's tint",
    "--accent-foreground": "base.css :root --accent-foreground",
    "--ring": "base.css :root --ring, the focus ring on the card's controls",
    "--ok": "base.css :root --ok",
    "--ok-subtle": "base.css :root --ok-subtle",
    "--warn": "base.css :root --warn",
    "--warn-subtle": "base.css :root --warn-subtle",
    "--danger": "base.css :root --danger, the colour sidebar-issues.css gives a critical row",
    "--danger-subtle": "base.css :root --danger-subtle",
    "--info": "base.css :root --info",
    "--info-subtle": "base.css :root --info-subtle"
  },
  "shape": {
    "--radius-sm": "6px",
    "--radius-md": "10px",
    "--radius-lg": "14px",
    "--radius-xl": "20px",
    "--radius-full": "9999px",
    "--shadow-sm": "0 1px 2px rgba(0, 0, 0, 0.25)",
    "--shadow-md": "0 4px 16px rgba(0, 0, 0, 0.3)",
    "--shadow-lg": "0 12px 32px rgba(0, 0, 0, 0.4)",
    "--ease-out": "cubic-bezier(0.16, 1, 0.3, 1)",
    "--duration-fast": "100ms",
    "--duration-normal": "180ms"
  },
  "motion": {
    "minVisibleMs": 900
  },
  "motionNote": [
    "minVisibleMs is the minimum-visible-duration primitive: the floor, in milliseconds, on how long a transient state stays on screen before it may be replaced, so a state change is understood rather than seen as a flash. It is read by core/ui/motion.js (remainingVisibleMs, heldLongEnough) on the desktop and mirrored by mobile/Chela/Motion.swift on iOS, so both clients hold a transient the same length; it is documented as design language in ui/CONVENTIONS.md ('a transient state is held long enough to read').",
    "It is OURS rather than borrowed from the Control UI, and it lives here rather than in ui.css because it is a value read by main-process JavaScript (the update lane floors its own state changes) and not only by a stylesheet: a CSS custom property cannot be read from Node. It is not one of the animation durations in shape above, which govern how a thing MOVES; this governs how long it STAYS. 900ms is the span of a glance that lands, reads a short sentence and confirms it; below it a changing state flashes.",
    "Because it is ours and not the Control UI's, it is exempt from the upstream-parity check the shape and colour tokens face (see core/test/motion.test.js, the exit-curve exception), and it is proven instead by core/test/motion.test.js against core/ui/motion.js and ui/CONVENTIONS.md, with mobile/ChelaTests/MotionParityTests.swift proving the iOS mirror answers the same."
  ],
  "shapeProvenance": {
    "--radius-sm": "base.css :root --radius-sm",
    "--radius-md": "base.css :root --radius-md, the card action's radius",
    "--radius-lg": "base.css :root --radius-lg, the card's own radius",
    "--radius-xl": "base.css :root --radius-xl",
    "--radius-full": "base.css :root --radius-full",
    "--shadow-sm": "base.css :root --shadow-sm, the attention card's resting shadow",
    "--shadow-md": "base.css :root --shadow-md, the toast's shadow",
    "--shadow-lg": "base.css :root --shadow-lg",
    "--ease-out": "base.css :root --ease-out",
    "--duration-fast": "base.css :root --duration-fast",
    "--duration-normal": "base.css :root --duration-normal"
  },
  "type": {
    "font": {
      "body": "\"Instrument Sans\", -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif"
    },
    "size": {
      "xs": "11px",
      "sm": "12px",
      "md": "14px",
      "lg": "16px"
    },
    "weight": {
      "headline": 650,
      "subject": 400,
      "action": 600
    },
    "leading": {
      "body": 1.55,
      "card": 1.35
    }
  },
  "typeProvenance": {
    "font.body": "base.css :root --font-body",
    "size.xs": "base.css --control-ui-text-xs at the default --control-ui-text-scale of 1, the subject line",
    "size.sm": "base.css --control-ui-text-sm at scale 1, the headline",
    "size.md": "base.css --control-ui-text-md at scale 1",
    "size.lg": "base.css --control-ui-text-lg at scale 1",
    "weight.headline": "sidebar-issues.css .sidebar-issues-panel__heading font-weight",
    "weight.subject": "base.css body weight, 400",
    "weight.action": "sidebar-issues.css .sidebar-issues-panel__action font-weight",
    "leading.body": "base.css body line-height",
    "leading.card": "sidebar-issues.css .sidebar-issues-panel__entity line-height"
  },
  "card": {
    "radius": "12px",
    "border": "--border",
    "surface": "--popover",
    "colour": "--popover-foreground",
    "shadow": "--shadow-md",
    "padding": "7px 7px 7px 13px",
    "gap": "8px",
    "maxWidth": "370px",
    "glyphSize": "16px",
    "messageSize": "13px",
    "leading": "1.5",
    "dismiss": {
      "size": "32px",
      "radius": "8px",
      "glyph": "14px",
      "colour": "--muted",
      "hoverSurface": "--bg-muted",
      "hoverColour": "--text-strong"
    },
    "action": {
      "minHeight": "32px",
      "padding": "5px 10px",
      "radius": "8px",
      "surface": "--accent-subtle",
      "colour": "--accent",
      "hoverSurface": "color-mix(in srgb, var(--accent) 16%, transparent)",
      "hoverColour": "--text-strong"
    },
    "phone": {
      "edge": "12px",
      "target": "44px"
    }
  },
  "cardProvenance": {
    "radius": "components.css .app-toast border-radius, the toast's own 12px rather than a shared radius token",
    "border": "components.css .app-toast border, the hairline at full strength",
    "surface": "components.css .app-toast background: the toast is OPAQUE, on --popover, with no blur. Until 2026-09-23 the banner copied the floating attention card (92% of --panel over a 10px blur); Abi asked for the Control UI's own notification style, which is this",
    "colour": "components.css .app-toast color",
    "shadow": "components.css .app-toast box-shadow",
    "padding": "components.css .app-toast padding: 13px on the icon side and 7px on the control side, so the X sits near the edge",
    "gap": "components.css .app-toast gap",
    "maxWidth": "components.css .app-toast max-width is min(370px, calc(100vw - 32px)). The banner takes the 370px and leaves out the viewport half: its page's viewport is the view sized FROM the card, and a card width that depends on the view is the loop that collapsed the banner to a sliver on Windows (2026-09-20)",
    "glyphSize": "components.css .app-toast__icon width and height: a 16px icon with no box around it",
    "messageSize": "components.css .app-toast__message font-size, 13px times --control-ui-text-scale",
    "leading": "components.css .app-toast__message line-height",
    "dismiss.size": "components.css .app-toast__dismiss width and height",
    "dismiss.radius": "components.css .app-toast__action, .app-toast__dismiss border-radius, the rule the two controls share",
    "dismiss.glyph": "components.css .app-toast__dismiss svg width and height",
    "dismiss.colour": "components.css .app-toast__dismiss color",
    "dismiss.hoverSurface": "components.css .app-toast__dismiss:hover background",
    "dismiss.hoverColour": "components.css the hover and focus-visible rule both toast controls share, color",
    "action.minHeight": "components.css .app-toast__action min-height",
    "action.padding": "components.css .app-toast__action padding",
    "action.radius": "components.css .app-toast__action, .app-toast__dismiss border-radius",
    "action.surface": "components.css .app-toast__action background: a filled accent button, with no border",
    "action.colour": "components.css .app-toast__action color",
    "action.hoverSurface": "components.css .app-toast__action:hover background",
    "action.hoverColour": "components.css the hover and focus-visible rule both toast controls share, color",
    "phone.edge": "components.css the phone @media rule: the toast spans the viewport 12px in from each side. Recorded rather than pinned, because the pin reads top-level rules only",
    "phone.target": "components.css the same @media rule: the action and the X grow to 44px touch targets"
  },
  "tone": {
    "error": {
      "edge": "--danger",
      "tint": "--danger-subtle"
    },
    "warn": {
      "edge": "--warn",
      "tint": "--warn-subtle"
    },
    "info": {
      "edge": "--info",
      "tint": "--info-subtle"
    },
    "ok": {
      "edge": "--ok",
      "tint": "--ok-subtle"
    }
  },
  "toneNote": [
    "One tone per notice, four tones, and all four are Control UI tokens rather than invented colours.",
    "error takes --danger because that is the colour sidebar-issues.css gives a critical attention row, not --destructive: --destructive is the brand red this app already paints everything with, so an error edge in it would be invisible.",
    "info takes --info rather than --accent for the same reason, and the Control UI has a real --info to take. It was --border-strong before this, which was quiet to the point of not being a tone at all.",
    "The card in the reference screenshot is ONE tone of the Control UI's attention card in one palette. The structure is what we share here (surface, radius, type, the dismiss and the action), so all four of our tones are styled from the same card rather than the violet case being the only one that exists.",
    "Which icon a tone draws is not here: it is core/spec/banner.json's toneIcon, the Control UI's own icon, which both banners draw in the card's icon box coloured by the edge token above. The tint is the action's hover wash."
  ]
}
