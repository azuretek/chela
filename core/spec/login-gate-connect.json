{
  "description": "Connect pressed on the Control UI's OWN login gate, the upstream page's 'Gateway unreachable' screen with its Connect button. This file is the ONE owner of the selectors that name that gate, the three reports a press produces, how long a client holds its loading cover for one, and the injected script that watches for the press. core/login-gate-connect.js reads it for the desktop, and the iOS client bundles this file and installs the same script.",
  "why": [
    "Reported from the macOS desktop build 1.0.1-dev.358 (Abi, 2026-09-25): \"when we get sent back to the can't connect to gateway error from the control UI when we hit connect it just hangs there for a while, we should do the load page there until the UI renders\". The screen is the Control UI's own: a reconnect of ours loads the page, the page's socket is refused, and the page draws its login gate with a network failure. Pressing its Connect pins the gate on screen while the page connects (app-root.ts, loginGatePinned), and the gate then sits there, unchanged, for as long as the attempt takes. Measured with desktop/scripts/test-login-gate-connect.js against a real gateway: the gate with nothing moving for the whole of a four-second connect.",
    "The page is upstream's and is not patched. Nothing here changes what the press does: the script listens in the capture phase and only REPORTS, so the page's own handler runs exactly as before, and a client that never installs this sees the page behave as it always did.",
    "The press rather than the socket. A socket opened while the gate is up is almost always the press, but the gate's own Connect is the one signal that is certainly the reader's, and the Enter key in its fields is the same submission (login-gate.ts calls onConnect for both). Reading the socket instead would also cover a page that retried on its own, which is a cover nobody asked for.",
    "Rendered means the gate has GONE, not that the socket opened. The page connects and only then replaces the gate with the interface, so the report waits for the gate to leave the document and then two animation frames, the same presented-frame rule core/spec/render-ready.json uses for a load.",
    "A failure is the gate's failure callout coming BACK. The press clears the previous failure while the attempt runs, so a callout still on screen at the moment of the press is the old one; only one that returns after it has gone is this attempt's answer. Its title travels with the report so the client's failure notice can say what the page said.",
    "deadlineMs bounds the whole thing, because a watcher that can only succeed is a cover that never lets go (core/ui/CONVENTIONS.md). A press with no answer by then lands on the loading cover's failed state with Try again, the path a failed launch takes. The script stops watching at the same moment, so a late answer cannot lift a cover the client has already failed.",
    "The reports travel on the pairing observer's channel (clawPairing on iOS, the __clawPairing global on the desktop) rather than a new one, because it is already the one thing the gateway page may say to this app, and a second channel would be a second surface for the page to reach.",
    "The selectors are upstream's names (ui/src/components/login-gate.ts), so a rename upstream would make the press invisible and the old hang would come back without an error. core/test/login-gate-connect.test.js checks them against the OpenClaw checkout when it is readable."
  ],
  "selectors": {
    "gate": "openclaw-login-gate",
    "connect": ".login-gate__connect",
    "fields": ".login-gate__form input",
    "failure": ".login-gate__failure",
    "failureTitle": ".login-gate__failure-title"
  },
  "upstreamComponent": "src/components/login-gate.ts",
  "reports": {
    "pressed": "connect-pressed",
    "rendered": "connect-rendered",
    "failed": "connect-failed"
  },
  "deadlineMs": 15000,
  "hook": [
    "(function () {",
    "  if (window.__clawLoginGateConnectInstalled) return;",
    "  window.__clawLoginGateConnectInstalled = true;",
    "  var GATE = 'openclaw-login-gate';",
    "  var CONNECT = '.login-gate__connect';",
    "  var FIELDS = '.login-gate__form input';",
    "  var FAILURE = '.login-gate__failure';",
    "  var FAILURE_TITLE = '.login-gate__failure-title';",
    "  var WATCH_MS = 15000;",
    "  function post(payload) {",
    "    try {",
    "      var handler = window.webkit && window.webkit.messageHandlers && window.webkit.messageHandlers.clawPairing;",
    "      if (handler) { handler.postMessage(payload); return; }",
    "    } catch (e) {}",
    "    try { window.__clawPairing = payload; } catch (e) {}",
    "  }",
    "  function within(event, selector) {",
    "    var path = typeof event.composedPath === 'function' ? event.composedPath() : [event.target];",
    "    for (var i = 0; i < path.length; i++) {",
    "      var node = path[i];",
    "      if (node && node.nodeType === 1 && typeof node.matches === 'function' && node.matches(selector)) return true;",
    "    }",
    "    return false;",
    "  }",
    "  // One press at a time. The watch ends on the first answer, or quietly once the",
    "  // host's own deadline has passed, and the host owns what happens then.",
    "  var watching = null;",
    "  function end() {",
    "    if (!watching) return null;",
    "    var w = watching;",
    "    watching = null;",
    "    try { w.observer.disconnect(); } catch (e) {}",
    "    clearTimeout(w.timer);",
    "    return w;",
    "  }",
    "  function check() {",
    "    if (!watching) return;",
    "    if (!document.querySelector(GATE)) {",
    "      // The gate has gone, so the interface is being drawn in its place. Two",
    "      // frames, so the frame that carries it has been presented before we say so.",
    "      end();",
    "      requestAnimationFrame(function () { requestAnimationFrame(function () { post({ kind: 'connect-rendered' }); }); });",
    "      return;",
    "    }",
    "    // A press clears the gate's previous failure while the attempt runs, so a",
    "    // failure is only this attempt's once the old one has been seen to go.",
    "    if (!document.querySelector(FAILURE)) { watching.cleared = true; return; }",
    "    if (!watching.cleared) return;",
    "    var titleNode = document.querySelector(FAILURE_TITLE);",
    "    var title = titleNode && typeof titleNode.textContent === 'string' ? titleNode.textContent.replace(/\\s+/g, ' ').trim().slice(0, 160) : '';",
    "    end();",
    "    post({ kind: 'connect-failed', title: title });",
    "  }",
    "  function pressed() {",
    "    if (watching || !document.querySelector(GATE)) return;",
    "    post({ kind: 'connect-pressed' });",
    "    watching = { cleared: false, observer: null, timer: 0 };",
    "    try {",
    "      watching.observer = new MutationObserver(check);",
    "      watching.observer.observe(document.documentElement, { subtree: true, childList: true });",
    "    } catch (e) {}",
    "    watching.timer = setTimeout(end, WATCH_MS);",
    "    setTimeout(check, 0);",
    "  }",
    "  document.addEventListener('click', function (event) {",
    "    if (within(event, CONNECT)) pressed();",
    "  }, true);",
    "  document.addEventListener('keydown', function (event) {",
    "    if (event.key === 'Enter' && !event.isComposing && within(event, FIELDS)) pressed();",
    "  }, true);",
    "})();"
  ]
}
